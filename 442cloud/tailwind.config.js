/**
 * Tailwind CSS v4
 * In v4 the design system lives in CSS via `@theme` (see src/app/globals.css),
 * and content sources are auto-detected. This file is kept minimal for tooling
 * compatibility; add `content` globs here only if you need to override detection.
 * @type {import('tailwindcss').Config}
 */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  plugins: [],
};
